/*
    투 포인터 알고리즘

     - 수열에서 두개의 포인터(인덱스)를 이동해 가면서 원하는 값을 찾아 내는 알고리즘이다.
     - 두 포인터의 구간 합이 원하는 값보다 작다면 endIdx를 늘려주고 반대라면 startIdx를 늘려준다.
     - 인덱스를 늘려주면서 구간 합도 계산한다.
     - 정렬이 안 되어 있다면 정렬 후에 계산을 해야 좋은 경우가 많다.
 */

